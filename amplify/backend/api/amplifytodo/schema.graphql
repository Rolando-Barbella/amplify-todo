type Todo @model {
  id: ID!
  name: String!
  description: String
}

type Tweet @model {
  id: ID!
  tweet: String!
  retweet: Int
  likes: Int
}

enum PostStatus {
  ACTIVE
  INACTIVE
}

type Post @model {
  id: ID!
  title: String!
  rating: Int!
  status: PostStatus!
  retweet: Int
}